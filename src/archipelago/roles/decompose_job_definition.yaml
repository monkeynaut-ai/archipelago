name: decompose_job_definition
description: Parses a job definition into flat state keys and an ordered list of commit slices
version: "1.0.0"
implementation:
  module: archipelago.agents.decomposer
  class_name: DecomposerHandler
tags:
  - archipelago
  - decomposer
quality_controls:
  timeout_seconds: 30
  max_retries: 0
